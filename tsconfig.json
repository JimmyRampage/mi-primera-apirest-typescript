{
  "compilerOptions": {
    /* Basic Options */
    "target": "ES2022",                       // Especifica la versión de ECMAScript de destino. ES2022 es moderno y compatible con Node.js v24.
    "module": "NodeNext",                      // El sistema de módulos a usar. 'NodeNext' es la opción moderna que soporta tanto CommonJS como ES Modules.
    "lib": ["ES2022"],                         // Librerías estándar que estarán disponibles. 'ES2022' coincide con nuestro target.
    "outDir": "./dist",                        // Carpeta de salida para los archivos JavaScript compilados.
    "rootDir": "./src",                        // Carpeta raíz que contiene nuestros archivos TypeScript.

    /* Strict Type-Checking Options */
    "strict": true,                            // Habilita todas las opciones de verificación de tipos estrictas. Es una buena práctica fundamental.
    "noImplicitAny": true,                     // Obliga a que todas las variables tengan un tipo explícito o inferido.
    "strictNullChecks": true,                  // Diferencia entre 'null', 'undefined' y otros tipos. Previene muchos errores comunes.

    /* Module Resolution Options */
    "moduleResolution": "NodeNext",            // Algoritmo para resolver módulos, alineado con 'module'.
    "baseUrl": ".",                            // Directorio base para resolver rutas de importación no relativas.
    "paths": {
      "@/*": ["src/*"]                         // Permite importaciones de alias. Por ejemplo, 'import x from "@/services/miServicio"' en lugar de '../../services/miServicio'.
    },
    "resolveJsonModule": true,                 // Permite importar archivos .json como módulos.

    /* Advanced Options */
    "esModuleInterop": true,                   // Mejora la interoperabilidad entre módulos CommonJS y ES Modules. Crucial para muchos paquetes de npm.
    "forceConsistentCasingInFileNames": true,  // Evita problemas de mayúsculas/minúsculas en los nombres de archivo, especialmente en diferentes sistemas operativos.
    "skipLibCheck": true                       // Omite la verificación de tipos de los archivos de declaración (.d.ts) de las dependencias. Acelera la compilación.
  },
  "include": ["src/**/*"],                     // Patrón que le dice a TypeScript qué archivos debe compilar.
  "exclude": ["node_modules", "**/*.spec.ts"]  // Patrón para excluir carpetas o archivos de la compilación.
}